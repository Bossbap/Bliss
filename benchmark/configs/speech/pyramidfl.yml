# Configuration file of FAR training experiment

# ========== Cluster configuration ========== 
# ip address of the parameter server (need 1 GPU process)
ps_ip: 129.104.253.51

# ip address of each worker:# of available gpus process on each gpu in this node
# Note that if we collocate ps and worker on same GPU, then we need to decrease this number of available processes on that GPU by 1
# E.g., master node has 4 available processes, then 1 for the ps, and worker should be set to: worker:3
worker_ips: 
    - 129.104.253.51:[3]
    - 129.104.252.53:[3]
    - 129.104.252.50:[4]
    - 129.104.252.38:[4]
    - 129.104.253.43:[4]
    - 129.104.253.47:[4]

exp_path: /users/eleves-a/2024/baptiste.geisenberger/FedScale/fedscale/cloud

# Entry function of executor and aggregator under $exp_path
executor_entry: execution/executor.py

aggregator_entry: aggregation/aggregator.py

auth:
    ssh_user: "baptiste.geisenberger"
    ssh_private_key: "/users/eleves-a/2024/baptiste.geisenberger/.ssh/id_rsa"

# cmd to run before we can indeed run FAR (in order)
setup_commands:
    - source /Data/baptiste.geisenberger/miniconda3/bin/activate fedscale
#    - export NCCL_SOCKET_IFNAME='enp0s31f6'         # Run "ifconfig" to ensure the right NIC for nccl if you have multiple NICs

# ========== Additional job configuration ========== 
# Default parameters are specified in config_parser.py, wherein more description of the parameter can be found

job_conf:
    - job_name: google_speech_pyramidfl
    - num_classes: 35
    - task: speech
    - log_path: /users/eleves-a/2024/baptiste.geisenberger/FedScale/benchmark
    - data_set: google_speech
    - data_dir: /Data/baptiste.geisenberger/Data/google_speech
    - data_map_file: /Data/baptiste.geisenberger/Data/google_speech/client_data_mapping/train.csv
    - clients_file: /users/eleves-a/2024/baptiste.geisenberger/FedScale/benchmark/dataset/data/clients.pkl
    - use_cuda: True
    - num_loaders: 2
    - clock_factor: 30
    - checkpoint_interval: 50
    - resume_from: "/users/eleves-a/2024/baptiste.geisenberger/FedScale/benchmark/logs/google_speech_pyramidfl/1028_215910/checkpoints"

    - model: resnet34
    - gradient_policy: fed-prox
    - proxy_mu: 0.001

    - num_participants: 100
    - eval_interval: 5
    - rounds: 500
    - local_steps: 30
    - batch_size: 16
    - filter_less: 17
    - learning_rate: 0.02
    - decay_factor: 0.9
    - decay_round: 10
    - weight_decay: 8e-5
    - test_bsz: 20
    - overcommitment: 1.3

    - sample_mode: pyramidfl
    - pacer_step: 20
    - pacer_delta: 10
    - exploration_factor: 0.9
    - exploration_decay: 0.98
    - exploration_min: 0.2
    - round_penalty: 2.0
    - round_threshold: 10
    - blacklist_rounds: -1
    - t_budget: 358

    - dropout_a: 0.1
    - dropout_b: 0.6
    - confidence_beta: 0.7
